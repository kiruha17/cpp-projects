# All Projects

| # | Project Name | Description | Requirements / Features |
|---|-------------|-------------|-------------------------|
| 1 | Resource Manager (RAII Demo) | Manage a resource (file, buffer, etc.) safely using RAII | • Create a class that acquires a resource in its constructor<br>• Release the resource in the destructor (RAII principle)<br>• Demonstrate exclusive ownership using `std::unique_ptr`<br>• Demonstrate shared ownership using `std::shared_ptr`<br>• Show behavior on copy vs move operations<br>• Prevent resource leaks and double frees |
| 2 | Custom String Class | Build a mini std::string from scratch | • Implement dynamic memory allocation (`new[]`, `delete[]`)<br>• Store character buffer and string length<br>• Implement copy constructor and copy assignment<br>• Implement move constructor and move assignment<br>• Overload `operator[]` for indexing<br>• Overload `operator+` for concatenation<br>• Implement destructor to clean up memory |
| 3 | CLI Tool (wc / grep) | Command-line text processor | • Accept file input via command-line arguments<br>• Read file using `std::ifstream`<br>• Count lines, words, and characters OR search for a pattern<br>• Handle invalid files or missing arguments<br>• Print formatted output to console<br>• Optionally support multiple files |
| 4 | In-Memory Database | Simple key-value store with query support | • Store data using `std::unordered_map` or similar container<br>• Support insert, update, delete operations<br>• Implement lookup by key<br>• Add filtering (e.g., find values matching conditions)<br>• Support iteration over stored data<br>• Optional: save/load data from file |
| 5 | Config File Parser | Parse INI/JSON-like configuration files | • Read configuration file into memory<br>• Parse key-value pairs from text<br>• Support nested sections or objects<br>• Handle malformed input gracefully<br>• Store parsed data in structured format (maps/structs)<br>• Provide API for querying values |
| 6 | Logging Library | Thread-safe logging system | • Implement logging levels (INFO, WARN, ERROR, DEBUG)<br>• Output logs to console<br>• Output logs to a file<br>• Add timestamps to log entries<br>• Ensure thread safety using mutexes<br>• Provide simple API (e.g., `log("message")`) |
| 7 | Task Scheduler / Thread Pool | Execute tasks concurrently | • Create a pool of worker threads<br>• Implement a thread-safe task queue<br>• Use `std::mutex` and `std::condition_variable` for synchronization<br>• Allow submission of tasks (functions/lambdas)<br>• Gracefully shut down all threads<br>• Ensure no race conditions or deadlocks |
| 8 | Game of Life / Simulation | Grid-based simulation | • Represent grid using 2D array or vector<br>• Implement update rules for each generation<br>• Efficiently compute next state without excessive copying<br>• Display grid state in console output<br>• Run simulation over multiple iterations<br>• Optional: optimize memory layout or performance |
| 9 | Basic HTTP Server | Minimal server handling HTTP requests | • Open a socket and listen on a port<br>• Accept incoming client connections<br>• Read and parse HTTP request text<br>• Handle simple GET requests<br>• Send valid HTTP responses<br>• Handle multiple requests sequentially or concurrently |
| 10 | Custom Memory Allocator | Implement simplified heap allocator | • Manage a contiguous block of memory<br>• Implement allocation (`malloc`-like function)<br>• Implement deallocation (`free`-like function)<br>• Track free and used memory blocks<br>• Handle memory fragmentation (basic merging of free blocks)<br>• Ensure alignment of allocations |
| 11 | CMake Project Setup | Structured multi-file project build system | • Organize code into multiple `.cpp` and `.h` files<br>• Write a `CMakeLists.txt` file<br>• Define executable targets<br>• Link libraries if needed<br>• Support debug and release builds<br>• Build project using CMake CLI |
